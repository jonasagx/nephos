#!/usr/bin/python3.4
from PIL import Image
import os

fileList = os.listdir()
s = [309, 334, 501, 522]

for f in fileList:
    if f.find("jpg") >= 0:
        im = Image.open(f)
        pralsamixer)
        im.crop(s).save(im.filename.replace(".jpg", "_square.jpg"))
        alsamixer
